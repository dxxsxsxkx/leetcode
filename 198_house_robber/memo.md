## Step 1

### 1回目

Bottom-up でやろうと思ったが、今ひとつ方針が立たなかった。

- `i` に押し入ると `i+1` には押し入れない。すると `i` に押し入るかどうかは `i+1` と `i+2` のどちらが大きいかによる？→ `i` と `i+2` の合計と比べなくていいのか？
  - `i` に押し入るかどうかの判断基準はどこに置けば良いのか？
- ここで答えを見た。

### 2回目

`i` 番目の家から盗む場合の最大金額を配列にする。配列の `i` 番目の要素は隣り合わない手前の要素のうち最大のものと `nums[i]` の和にする。最後は `nums.size() - 1` を盗む場合ないし `nums.size() - 2` を盗む場合のいずれか大きい方を返す。

- コードは `step1.cpp` に書いた。
- 計算量：時間 $O(N^2)$、空間 $O(N)$。
- [参照](https://github.com/5103246/LeetCode_Arai60/pull/33/changes/2d5fc40c915f3eb2b7c3c319c6320d68c2c247a6#diff-b5d0021bcd923135a6cb734d85019f54bd9017833447f559be013d4caf265c36R28-R42:~:text=%2D%20%5B1%2C%202%2C%203%2C%204%5D%E3%81%AA%E3%82%89%E3%80%81%5B1%2C%202%2C%204%2C%206%5D%E3%81%AE%E3%82%88%E3%81%86%E3%81%AB%E3%80%81i%20%2D%201%E4%BB%A5%E5%89%8D%E3%81%AE%E8%A8%88%E7%AE%97%E7%B5%90%E6%9E%9C%E3%81%AE%E4%B8%AD%E3%81%A7%E6%9C%80%E5%A4%A7%E3%81%AE%E3%82%82%E3%81%AE%E3%82%92%E6%8E%A2%E3%81%97%E3%81%A6%E3%81%9D%E3%82%8C%E3%81%AE%E5%90%88%E8%A8%88%E5%80%A4%E3%82%92%E5%85%A5%E3%82%8C%E3%81%A6%E3%81%84%E3%81%8F%E3%80%82)

## Step 2

### 勉強

同じように table を作る。端から見ていって、`i - 1` を取るか `i - 2` に `i` を足したものを取るかで前に進んでいく。こちらの方がわかりやすい上に、計算量も少ない。

- `step2.cpp` に書いた。
  - 書いてから、`.back()` の方がシンプルだと思った
- 時間計算量 $O(N)$、空間計算量 $O(N)$。
- [参照](https://github.com/Ryotaro25/leetcode_first60/pull/36/changes#diff-aea67cccd6e098417b4984598d8fe3717c1257e899a240a41cfb95b6708e2f45)
- Table を作る方法が選択肢としてパッと思い浮かぶと良いだろう。

その上で、`std::vector` を作って記録する必要は必ずしもない。必要なのは1個前と2個前の情報だけ。

- 泥棒が2つだけ数字の書いてあるメモを更新していく。`i`番目の家の前で2つの数字を見て、その家に押し入ろうかどうかを考える感じ。
- こうするとメモリが節約できる（空間計算量 $O(1)$）。
- `without_vector.cpp` に書いた。

## Step 3

最大金額の配列を持つアプローチが一番わかりやすいと感じたので、それで `step3.cpp` に書いた。
